[project]
name = "ai-daily-digest"
version = "0.1.0"
description = "AI Daily Digest — hybrid Python + Claude Code RSS digest for Obsidian"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.9.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
