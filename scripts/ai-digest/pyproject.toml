[project]
name = "ai-daily-digest"
version = "0.1.0"
description = "AI Daily Digest — Bedrock-powered RSS summarizer for Obsidian"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.42.63",
    "aiohttp>=3.9.0",
    "trafilatura>=1.8.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
