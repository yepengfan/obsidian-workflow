Scan the note at the path I provide (or the most recently modified note if none given) and add `[[wikilinks]]` to all meaningful references:

- People, places, books, projects, and concepts that have existing notes in the vault
- Search the vault for matching notes before linking
- Only add links to notes that actually exist in the vault
- Do NOT create new notes, just add links to existing ones
- Do NOT modify any files in `WeRead/`
- Preserve all existing content and formatting

Report what links you added when done.

$ARGUMENTS
